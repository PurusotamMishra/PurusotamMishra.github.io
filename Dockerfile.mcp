# Base image with Python 3.11
FROM python:3.11-slim

# Set environment variables
ENV DEBIAN_FRONTEND=noninteractive

WORKDIR /app
# Install system dependencies and Python
RUN apt-get clean && \
    apt-get update --fix-missing && \
    apt-get install -y --no-install-recommends \
    vim \
    cron \
    less \
    && rm -rf /var/lib/apt/lists/*

# # Install Python dependencies
COPY ./mcp-server/requirements.txt /app/mcp-server/requirements.txt
RUN pip3 install --no-cache-dir -r /app/mcp-server/requirements.txt

COPY ./mcp-server /app/mcp-server

# # Expose ports
EXPOSE 8000

# Start both server in development mode
CMD ["sh", "-c", "cd /app/mcp-server &&  python3 run.py"]